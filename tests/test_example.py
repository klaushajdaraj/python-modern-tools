def add_numbers(a: float, b: float) -> float:
    """Add two numbers and return the result."""
    return a + b

# Test case for the function
def test_add_numbers():
    assert add_numbers(2, 3) == 5  # Test with positive integers